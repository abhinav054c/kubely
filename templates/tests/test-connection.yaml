apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "callmatic.fullname" . }}-test-connection"
  labels:
    {{- include "callmatic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "callmatic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
